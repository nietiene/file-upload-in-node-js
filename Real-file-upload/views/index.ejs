<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Uploader</title>
      <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center p-6">
    <div class="w-full max-w-xl bg-white rounded-lg shadow-lg p-6">
       <h1 class="text-2xl font-bold mb-4 text-center">Upload Image</h1>
        <form action="/upload" method="post" enctype="multipart/form-data"
        class="flex flex-col gap-4">
           <input type="file" name="image" required
           class="block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4
           file:rounded-full file:border-0">
           <button type="submit">Upload</button>
       </form>

    </div>
  
    <h2>Uploaded Image</h2>
    <ul>
        <% image.forEach(img => { %>
          <li>
            <img src="/uploads/<%= img.file_name %>" width="100"> <br>
            <a href="/download/<%= img.file_name %>">Download</a>
            <a href="/delete/<%= img.id %>" onclick="return confirm('Delete this image')">Delete</a>
          </li>
         <% }) %>
    </ul>
</body>
</html>